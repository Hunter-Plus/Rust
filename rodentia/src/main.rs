// Using paths
use crate::sciuridae::squirrel::PrairieDog;

// Declaring modules
pub mod aplodontiidae; // src/[name]/mod.rs
pub mod sciuridae; // src/[name].rs
mod gliridae {} // inline

fn main() {
    println!("Hello, world!");
}
